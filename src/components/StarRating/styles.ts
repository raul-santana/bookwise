import { styled } from '../../../stitches.config'

export const StarRatingContainer = styled('div', {
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
  gap: '0.25rem',

  svg: {
    color: '$purple100',
  },
})
