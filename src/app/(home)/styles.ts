import { styled } from '../../../stitches.config'

export const HomeContainer = styled('section', {
  flex: 1,
  marginTop: '4.5rem',
  padding: '0 2rem',
})

export const ContentContainer = styled('section', {
  // width: '100%',

  marginTop: '2.5rem',

  display: 'flex',
  gap: '4rem',
})

export const RecentsRatingsContainer = styled('div', {
  flex: 1,
  display: 'flex',
  flexDirection: 'column',
  gap: '4rem',
})
